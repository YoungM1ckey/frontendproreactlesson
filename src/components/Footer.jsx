import React from 'react';
import styles from './Footer.module.css';

function Footer() {
    return (
        <footer className={styles.footer}>
            <small>&copy; 2025 Emoji Voting App</small>
        </footer>
    );
}

export default Footer;
